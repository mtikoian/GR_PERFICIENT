 The following changes are to be made by this deployment.

1. Update the Gr.FunctionalDepartmentExpanded function.
2. (Only on staging) Update the BudgetsToProcess stored procedure to point to the GDM_GR database.
3. Change the PayrollDataPriorToDate with the Data Inserts and Updates script.
4. (Only on staging) Change the GDM and GdmSnapshot connection string to point to the GDM_GR database. 